<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:name=".PolicyAlarmApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PolicyAlarm">

        <!-- 백그라운드/종료 상태 FCM 기본 채널 — IMPORTANCE_HIGH 보장 -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="policy_alerts_v2"/>

        <!-- AdMob 앱 ID (AdMob 콘솔 > 앱 설정) -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-4710152968528474~4270535612"/>

        <!-- AdMob과 Analytics가 AD_SERVICES_CONFIG 속성을 각각 정의하므로 앱 선언으로 덮어씀 -->
        <property
            android:name="android.adservices.AD_SERVICES_CONFIG"
            android:resource="@xml/gma_ad_services_config"
            tools:replace="android:resource"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!--
                Android App Links — 웹(policy-alerm.web.app)에서 공유된 정책 링크를
                앱이 설치돼 있으면 브라우저 대신 앱 상세 화면으로 연다.
                autoVerify는 https://policy-alerm.web.app/.well-known/assetlinks.json 과
                앱 서명 SHA-256 지문이 일치해야 동작한다(불일치 시 링크는 그냥 브라우저로 감).
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https"
                    android:host="policy-alerm.web.app"
                    android:pathPrefix="/policy/"/>
            </intent-filter>
        </activity>

        <service
            android:name=".service.PolicyFcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>
    </application>
</manifest>
